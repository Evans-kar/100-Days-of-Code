//Write a function that takes two numbers as parameters and returns their sum.
function sum (num1,num2) {
    return num1 + num2; //sum of the two numbers
}
// 3. Call the function with two numbers and store the result
let result = sum(5, 7);

// 4. Print the result to the console
console.log(result);