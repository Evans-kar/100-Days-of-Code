//arithmetic operators
let a = 3;   //declared variable a and initialized it 
let x = (100 + 50) * a; // arithmetic operators used for addition and multiplication
console.log (x);
//string concatenation 
let text1 ="Today was a good day"; //string
text1 += "since the sunny was out wish I knew how to swim!"; //+= is used for string concatenation
console.log(text1);
//assignment operators
console.log(x%a);
console.log(x**a);
console.log(x+=a);
//&& logical add operator if the first value is true the second value is assigned
let m = true; // declared and initialized variable m
let n = m &&= 10; //declared and initialized variable n
console.log(n); //testing the operand
//||= logical add operator if the first value is false the second value is assigned
let y = true; // declared and initialized variable m
let z = y &&= 10; //declared and initialized variable n
console.log(z); //testing the operand